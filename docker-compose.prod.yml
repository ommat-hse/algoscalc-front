version: '3.7'

services:

  front_prod:
    container_name: FrontProd
    restart: always
    build:
      context: .
      dockerfile: Dockerfile.prod
    networks:
      nginx_net1:
        ipv4_address: 172.77.0.4

networks:
  nginx_net1:
    external:
      name: nginx-net
