version: '3.7'

services:

  front_test:
    container_name: FrontTest
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    networks:
      nginx_net1:
        ipv4_address: 172.77.0.2

networks:
  nginx_net1:
    external:
      name: nginx-net
